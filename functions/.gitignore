node_modules/
*.local

# Generated by `npm run emails:build` (bundles React Email + deps via esbuild).
# Source of truth is emails/src/; rebuilt automatically via firebase.json predeploy hook.
emails/*.cjs