iOS install screenshots live under **`../iphone-add-to-homescreen/`** (not this folder):

- `1.png` — step 1 (menu → Share)
- `2.png` — step 2 (Add to Home Screen)
- `3.png` — step 3 (Add / +)

`InstallAppCard` and `DashboardInstallEngageBanner` render these via `IosInstallScreenshotGallery`; missing images are skipped automatically.
