node_modules
.env
.env.*
!.env.example
.DS_Store
dist
# Cursor agent per-session debug logs (pattern covers any session id).
.cursor/debug-*.log
mcp.json
# Vercel CLI local metadata (created by `vercel link` / `vercel pull`).
.vercel/
# Test coverage output (vitest --coverage, nyc, etc.).
coverage/
firestore-debug.log
firebase-debug.log
*.log

# Local credentials (GCP service accounts, etc.). Prefer ~/… or secrets/ and never commit.
secrets/
