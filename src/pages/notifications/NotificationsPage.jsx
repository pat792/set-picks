import React from 'react';

import { NotificationsPrototypeScreen } from '../../features/notifications';

/**
 * Route shell for notification preferences / inbox (prototype).
 * Keeps Firestore and orchestration in `features/notifications` when implemented.
 */
export default function NotificationsPage() {
  return <NotificationsPrototypeScreen />;
}
