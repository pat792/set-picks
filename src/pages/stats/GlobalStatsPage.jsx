import React from 'react';

import { GlobalStatsComingSoon } from '../../features/stats';

/**
 * Stats cluster — Global Stats (`/dashboard/stats/global`).
 * Phase 1 rankings-coming shell (#1004). Song explorer lives on Band.
 */
export default function GlobalStatsPage() {
  return <GlobalStatsComingSoon />;
}
