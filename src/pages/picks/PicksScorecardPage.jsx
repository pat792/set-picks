import React from 'react';

import { PicksScorecardPlaceholder } from '../../features/picks';

/**
 * Picks cluster — Scorecard shell (`/dashboard/picks/scorecard`).
 * Metrics belong to a sibling issue; this page is the empty destination.
 */
export default function PicksScorecardPage() {
  return <PicksScorecardPlaceholder />;
}
