---
description: After every staging→main promote, tag and publish the GitHub Release — no separate ask required
alwaysApply: true
---

# Promote → tag / release (required)

When an agent **merges or completes a promote** (`staging` → `main`, typically `promote/X.Y.Z`), they **MUST** publish the SemVer release on `main` in the same turn — do **not** wait for a separate “tag it” request.

```bash
git checkout main && git pull origin main
npm run release:publish -- --confirm
```

That runs release-gate → annotated tag `vX.Y.Z` → push tag → `gh release create` (notes from `CHANGELOG.md`).

## Guards

- Tag **only** on `main` after the promote merge lands (never mid-train, never on feature/`staging`-only tips).
- Skip if `package.json` version already matches the latest GitHub Release tag (gate will fail — do not force).
- Dependabot-only promotes with `skip-version-bump` and no version change: do not invent a tag.
- Never retag or move an existing `vX.Y.Z`; ship a new version instead.
- Sync `staging` to `main` after promote when that is already part of the promote workflow.

## Related

- `.cursorrules` §10 (SemVer) · `.cursor/rules/versioning.mdc` · `npm run release:gate`
